import React from 'react';

import Layout from '../../components/layout';

const Planting = () => {
  return (
    <Layout>It's when someone who's not that smart tries to plant.</Layout>
  );
};

export default Planting;
